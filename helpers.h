#ifndef __HELPERS_H
#define __HELPERS_H

// This function tokenisez the input sting on the basis of the delimeter passed and stores the
// tokenised string into token and holds a count of the no. of tokenised strings
void tokenizer(char *token[], char *s, char *delimParameter, long long int *total);  


#endif